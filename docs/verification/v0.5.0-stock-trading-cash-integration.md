# v0.5.0 Stock Trading and Cash Integration Verification

Date: 2026-08-18

## Delivered

- Added append-only manual stock buy/sell trades.
- Added schema v5 stock trade persistence and v4 migration coverage.
- Added same-currency cash ledger synchronization.
- Added Taiwan individual-stock quantity, price, and weighted average-cost updates.
- Kept Taiwan ETF and US stock accounts in principal-only mode.
- Added atomic Drift persistence for trade, holding, and ledger writes.
- Added buy/sell actions and read-only trade history to the assets page.
- Kept trade edits, deletion/voiding, realized gain/loss, fees, taxes, dividends, splits, FX conversion, and automatic quotes out of v0.5.

## Verification commands and results

| Command | Result |
| --- | --- |
| flutter test | PASS — 187 tests |
| flutter analyze | PASS — No issues found |
| flutter build apk --release | PASS — build/app/outputs/flutter-apk/app-release.apk (57.5 MB) |
| flutter build ios --simulator | PASS — build/ios/iphonesimulator/Runner.app |
| git diff --check | PASS |

Focused coverage includes trade domain validation, schema v5 persistence,
migration preservation, weighted-cost and principal calculations, currency
validation, atomic executor writes, trade history, and existing assets and
overview widget flows.

## Data compatibility

The v4-to-v5 migration creates the stock_trades table and preserves existing
cash ledger rows, stock accounts, and v0.4 stock holding snapshots without
backfill.

## Repository state

All v0.5 implementation milestones were committed and pushed to origin/main.
The local iOS signing change in ios/Runner.xcodeproj/project.pbxproj remains
intentionally uncommitted.
