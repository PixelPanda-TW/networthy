# Flutter 基礎記帳 App MVP 規格

- 版本：0.3
- 日期：2026-08-10
- 狀態：待使用者審閱
- 平台：iOS、Android

## 1. 產品摘要

本產品是一款以 Flutter 開發、離線優先的個人記帳 App。V1 不要求註冊、不依賴網路，讓使用者能快速記錄收入與支出，查看每月收入、支出、結餘及支出分類。本機帳務資料庫必須加密；使用者可以自行開啟生物辨識 App 鎖。

### 1.1 MVP 成功條件

- 一般使用者可在約 10 秒內新增一筆支出。
- 沒有網路時，所有核心功能仍可使用。
- App 重啟後資料仍存在，且資料庫檔案無法直接讀出明文帳務。
- 月收入、月支出、月結餘和分類加總結果正確。
- iOS 與 Android 的主要流程皆可正常操作。

### 1.2 目標使用者

希望簡單記錄個人日常收支、不需要多人共享、銀行串接或複雜財務分析的使用者。

## 2. V1 範圍

### 2.1 本月總覽

首頁顯示：

- 選定月份的總收入、總支出與結餘。
- 支出分類金額及占比。
- 最近五筆收支紀錄。
- 新增收支的主要操作按鈕。

使用者可以前後切換月份。月份切換後，上述數值與列表必須同步更新。

`月結餘 = 當月收入總額 - 當月支出總額`

### 2.2 新增與編輯收支

| 欄位 | 必填 | 規則 |
| --- | --- | --- |
| 類型 | 是 | 收入或支出；新增時預設為支出 |
| 金額 | 是 | 大於 0、不超過 999,999,999 TWD |
| 分類 | 是 | 必須屬於所選類型 |
| 日期 | 是 | 預設為裝置所在地的今天 |
| 備註 | 否 | 0–100 個 Unicode 字元 |

互動要求：

- 開啟新增頁後直接聚焦金額欄位並使用數字鍵盤。
- 金額顯示千分位，但儲存時使用整數。
- 支出新增成功後，記住最後使用的支出分類；下次新增支出時預選該分類。
- 收入與支出的「最後分類」分開記錄，避免跨類型套用錯誤分類。
- 儲存成功後返回來源頁並立即刷新統計。
- 儲存失敗時保留輸入內容，顯示可理解且不含敏感細節的錯誤訊息。

### 2.3 收支紀錄列表

- 預設依交易日期由新到舊排列；同日資料依建立時間由新到舊排列。
- 可依月份篩選。
- 可切換全部、收入或支出。
- 點擊紀錄可進入編輯。
- 可刪除紀錄；刪除前必須確認。
- 編輯或刪除後，相關月份統計必須立即更新。

### 2.4 預設分類

支出分類：餐飲、交通、購物、居住、娛樂、醫療、教育、其他。

收入分類：薪資、獎金、投資、其他。

分類以穩定 ID 儲存，顯示名稱由 App 的本地化資源提供，避免日後更名破壞舊資料。V1 不允許新增、刪除或排序分類。

### 2.5 設定

- 開啟或關閉生物辨識 App 鎖；預設關閉。
- 顯示 App 版本。
- 顯示離線儲存與資料遺失風險的隱私說明。
- 清除全部資料。

清除資料必須經過二次確認。若 App 鎖已開啟，清除前還要通過系統身分驗證。清除成功後必須刪除帳務資料庫、資料庫金鑰與相關偏好設定，再回到初次使用狀態。

## 3. 導覽與畫面

底部導覽列包含：總覽、紀錄、設定。總覽及紀錄頁皆提供新增收支按鈕。

必要畫面：

1. 一頁式初次使用說明。
2. 月度總覽。
3. 新增／編輯收支共用表單。
4. 收支紀錄列表。
5. 設定。
6. App 鎖定畫面。
7. 清除資料確認視窗。
8. 無資料、載入及錯誤狀態。

V1 的初次使用說明只解釋「資料保存在本機、解除安裝可能無法復原」並提供開始按鈕，不要求使用者先設定 App 鎖。

## 4. 資料模型

### 4.1 Transaction

| 欄位 | 型別 | 約束 |
| --- | --- | --- |
| id | UUID 字串 | 主鍵，不可變 |
| type | enum | `income` 或 `expense` |
| amountMinor | 64-bit integer | 大於 0；TWD 的 minor unit 為 1 元 |
| currencyCode | 字串 | V1 固定為 `TWD` |
| categoryId | 字串 | 必須與 type 相容 |
| transactionDate | 本地日曆日期 | 僅表示年月日，不以 UTC 時刻取代 |
| note | nullable 字串 | 最多 100 個 Unicode 字元 |
| createdAtUtc | UTC timestamp | 建立時間 |
| updatedAtUtc | UTC timestamp | 最後修改時間 |

使用 64-bit integer 儲存金額，不使用浮點數。交易日期是使用者選擇的日曆日期；建立與更新時間使用 UTC，避免跨時區後交易日期被意外移動。

### 4.2 AppSettings

| 欄位 | 型別 | 預設值 |
| --- | --- | --- |
| biometricLockEnabled | bool | false |
| currencyCode | 字串 | TWD |
| lastExpenseCategoryId | nullable 字串 | null |
| lastIncomeCategoryId | nullable 字串 | null |
| onboardingCompleted | bool | false |

資料庫金鑰不屬於一般設定，不得放進 SharedPreferences 或同等的一般鍵值儲存。

### 4.3 V2 同步預留

V1 不新增無用途的使用者資料表，但資料存取必須經過 repository 介面。V2 加入帳號時，可為交易增加 `userId`、同步狀態與伺服器版本欄位；UI 不直接操作資料庫。

## 5. 安全與隱私設計

### 5.1 威脅模型

V1 防護目標：

- 避免從 App 沙盒取得資料庫檔案後直接讀出帳務明文。
- 避免資料庫金鑰出現在原始碼、一般偏好設定、分析事件、crash report 或日誌。
- App 鎖啟用時，避免他人在已解鎖手機上直接查看帳務。

V1 不宣稱能抵禦已 root／jailbreak、遭惡意程式完整控制，或攻擊者能在 App 解密資料後讀取程序記憶體的裝置。

### 5.2 資料庫加密

1. 首次啟動時，以密碼學安全亂數產生至少 256-bit 的資料庫金鑰。
2. 金鑰以二進位安全格式編碼後，存入 iOS Keychain／Android Keystore 保護的安全儲存。
3. 以支援 SQLCipher 或 SQLite3MultipleCiphers 的 SQLite 建置開啟資料庫，並在任何 schema 查詢前設定金鑰。
4. 開啟後執行最小查詢，確認金鑰正確且實際使用預期的加密引擎；驗證失敗不得繼續載入帳務畫面。
5. 正式版不得輸出 SQL 綁定值、交易備註、金額、金鑰或完整資料庫路徑。

WAL、journal 和暫存檔也必須納入真機檔案檢查，確認不含帳務明文。

### 5.3 金鑰遺失與重置

- 資料庫存在但金鑰不存在或無法使用時，App 不得自動產生新金鑰覆蓋現況。
- App 應顯示「本機資料目前無法解鎖」並提供重試或清除後重新開始。
- 清除操作必須明確告知資料無法復原。
- V1 不提供復原碼、匯出或雲端備份。

### 5.4 生物辨識 App 鎖

- 預設關閉，由使用者在設定中開啟。
- 開啟前檢查裝置是否支援且已註冊生物辨識。
- 允許系統 PIN、圖形或裝置密碼作為備援驗證。
- App 冷啟動時，以及進入背景超過 30 秒後返回前景時要求驗證。
- 背景與 App 切換器預覽不得顯示帳務內容，應以遮罩覆蓋。
- 驗證取消或失敗時停留在鎖定畫面，可再次嘗試或離開 App。
- 生物辨識只控制畫面存取，不作為資料庫金鑰來源。

## 6. 技術架構與套件方向

### 6.1 分層

- `presentation`：頁面、元件、狀態呈現。
- `application`：新增、編輯、刪除、月度彙總及 App 鎖流程。
- `domain`：Transaction、分類、金額與日期規則。
- `data`：Drift DAO、repository 實作、安全金鑰儲存。
- `platform`：生命週期、生物辨識及背景畫面遮罩。

依賴方向由外向內；domain 不依賴 Flutter 或資料庫套件。畫面只呼叫 use case／repository 抽象，不直接執行 SQL。

### 6.2 建議選型

| 目的 | 建議 | 備註 |
| --- | --- | --- |
| UI | Flutter Material 3 | iOS、Android 共用專案 |
| 關聯式資料層 | Drift | 型別安全查詢、migration、reactive stream |
| SQLite binding | sqlite3 3.x | 使用 build hooks 接入加密 SQLite 實作 |
| 全庫加密 | SQLCipher 或 SQLite3MultipleCiphers | 必須先完成雙平台真機技術驗證再固定 provider |
| 金鑰安全儲存 | flutter_secure_storage | iOS Keychain／Android 安全儲存 |
| 本機驗證 | local_auth | 生物辨識及系統認證 |
| 一般偏好 | shared_preferences 或同級方案 | 僅存非敏感設定 |

不得採用已失效的 `sqlcipher_flutter_libs` 作為 sqlite3 3.x 的必要依賴。開始正式功能前，先做一個最小技術 spike：iOS 與 Android 各建立、關閉、重開加密資料庫，並用外部工具確認檔案、WAL 與 journal 沒有測試明文。若某 provider 無法穩定支援兩平台，才評估另一個 provider。

套件版本在建立專案時鎖定並提交 lockfile；升級加密或安全儲存套件時必須執行資料相容性與 migration 測試。

## 7. 錯誤處理

- 表單驗證錯誤顯示在對應欄位旁。
- 資料庫暫時錯誤顯示重試操作，不清空使用者尚未儲存的表單。
- 無法解密與一般讀寫失敗使用不同錯誤狀態，避免誤導使用者清除資料。
- 設定生物辨識失敗時保持原本開關狀態。
- 裝置不支援或未設定生物辨識時，不允許開啟 App 鎖，並引導使用者前往系統設定。
- 所有記錄系統必須對敏感內容進行省略或遮蔽。

## 8. 測試與驗收

### 8.1 單元測試

- 金額上下限及非法輸入。
- 收入／支出分類相容性。
- 月收入、月支出、結餘與分類占比。
- 月界線、閏日與時區變化不改變交易日期。
- repository 的新增、查詢、修改與刪除。
- App 鎖狀態機及 30 秒背景門檻。

### 8.2 資料庫測試

- 首次建立與使用既有金鑰重開。
- 錯誤金鑰無法讀取資料。
- schema migration 保留既有資料。
- 刪除交易後彙總同步更新。
- 資料庫、WAL、journal 及暫存檔不含測試明文。
- 金鑰遺失時不會自動覆蓋資料庫。

### 8.3 Widget／整合測試

- 新增、編輯、刪除的主要流程。
- 月份與類型篩選。
- 空白狀態與錯誤狀態。
- 開啟、關閉 App 鎖。
- App 從背景返回的鎖定與預覽遮罩。
- 清除全部資料及回到初次使用狀態。

### 8.4 真機驗收

- 至少一台受支援 iPhone 與一台受支援 Android 手機。
- Face ID／Touch ID／Android 生物辨識與系統憑證備援。
- 飛航模式下完成全部核心流程。
- 強制關閉 App 後重啟，帳務仍可解密並正確顯示。
- App 鎖啟用時，未驗證前看不到帳務或 App 切換器快照。
- Release build 不輸出敏感日誌。

## 9. 非功能需求

- 首頁在一般資料量（10,000 筆交易以內）下應於 1 秒內顯示本機資料；此為產品目標，需用真機量測。
- 所有可點擊控制項應符合平台基本無障礙尺寸，並提供語意標籤。
- 支援系統文字縮放，主要流程不得因一般文字放大而無法操作。
- V1 介面語言為繁體中文；資料結構不得依賴顯示文字作為 ID。
- V1 單一幣別為 TWD，顯示格式為 `NT$12,500`。

## 10. 明確不做

- 註冊、登入、後端及雲端同步。
- 多使用者、家庭帳本或多裝置。
- 備份、還原、匯入與匯出。
- 銀行、信用卡或發票串接。
- 預算、固定週期收支及通知。
- 多帳戶、多幣別與匯率換算。
- 自訂分類、全文搜尋及 AI 自動分類。
- 投資或資產管理。

## 11. V2 方向

V2 可加入帳號、Apple／Google 登入、雲端同步、換機復原及帳號刪除。同步設計必須另外定義：端到端加密取捨、裝置金鑰管理、離線佇列、衝突合併、刪除傳播與 V1 本機資料的首次上傳流程，不納入本規格。

## 12. 發布驗收清單

1. iOS 與 Android 可安裝並正常啟動。
2. 無網路時可新增、查看、修改及刪除收支。
3. 月度與分類統計正確。
4. 重啟後資料仍存在且能解密。
5. 本機資料庫及旁支檔案無帳務明文。
6. 金鑰未出現在原始碼、設定、日誌或分析事件。
7. 生物辨識鎖可正常開關，系統憑證備援可用。
8. 未解鎖前不顯示帳務或背景快照。
9. 清除全部資料會移除資料庫、金鑰與設定。
10. 主要畫面無溢出，螢幕閱讀器可辨識主要操作。

## 13. 技術資料來源

- [Flutter `local_auth`](https://pub.dev/packages/local_auth)：Flutter 官方維護的本機生物辨識／裝置認證插件。
- [`flutter_secure_storage`](https://pub.dev/packages/flutter_secure_storage)：提供 iOS Keychain 與 Android 平台安全儲存整合。
- [Drift](https://pub.dev/packages/drift)：以 SQLite 為基礎的型別安全 reactive persistence library。
- [Dart `sqlite3`](https://pub.dev/packages/sqlite3)：3.x 支援透過 build hooks 使用 SQLCipher 或 SQLite3MultipleCiphers。
- [SQLCipher security design](https://www.zetetic.net/sqlcipher/design/)：主資料庫、journal 與 WAL 的加密設計說明。

## 14. 已確定的產品決策

- V1 是 Flutter iOS／Android App。
- V1 為單一使用者、免註冊、完全本機儲存。
- 支援收入與支出。
- 本機資料庫必須加密。
- 生物辨識 App 鎖為可選功能，預設關閉。
- 允許系統 PIN／圖形／裝置密碼作為備援。
- V1 解除安裝或金鑰遺失後不保證資料復原。
